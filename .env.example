# --- Dytor backend ---
# Where the website's /auth/desktop/callback POSTs the Supabase access token.
NEXT_PUBLIC_DYTOR_BACKEND_URL=http://localhost:4000

# Project Settings → API → "anon public" key
NEXT_PUBLIC_SUPABASE_ANON_KEY=
